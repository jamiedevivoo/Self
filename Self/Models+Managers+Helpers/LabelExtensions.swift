import Foundation
import  UIKit

extension UILabel {
    static var title: UILabel {
        let label = UILabel()
        label.text = "Title"
        return label
    }
    
    static var disabled: UILabel {
        let label = UILabel()
        return label
    }
    
    func updateText() -> UILabel {
        let label = UILabel()
        return label
    }
}
